# Privacy and security

Analysis is local and offline. Codebook Cinema neither uploads repositories nor calls a language model. Reports retain paths, symbol names, and import statements because these are the evidence behind every generated claim. Secret-looking assignment values are replaced with `[REDACTED]` in HTML and Markdown excerpts.
