# Contributing

Run `python -m pip install -e '.[dev]'`, then `make verify` before opening a pull request.
Keep descriptions evidence-linked: never invent a symbol or infer a business purpose without marking it `unknown`.
